const search = document.getElementById("user_input");

// on key up, autocomplete
// on submit, redirect to search result

// search.addEventListener('submit', submitSearch)
// search.addEventListener('change', autocomplete)
